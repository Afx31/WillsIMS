{
  "name": "WillsIMS",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "",
    "prestart": "node ./api/aspnetcore-https && node ./api/aspnetcore-react",
    "start": "rimraf ./build && react-scripts start",
    "build": "react-scripts build",
    "test": "cross-env CI=true react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "lint": "eslint ./src/"
  },
  "dependencies": {
    "http-proxy-middleware": "^2.0.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.11.0",
    "react-scripts": "^5.0.1",
    "rimraf": "^5.0.5",
    "rixun-ui": "^1.0.4"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^8.39.0"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
